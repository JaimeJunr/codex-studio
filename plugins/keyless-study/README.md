# keyless-study

Especialista **estudo** sobre o `keyless-deck`: **deck de aula, resumo visual, flashcards em imagem e defesa/apresentação acadêmica**.

## Skill
- **`keyless-study:study-deck`** — estrutura pedagógica (objetivo → conteúdo → checagem); delega geração/montagem ao `keyless-deck`. Em defesa, insere as figuras reais do trabalho.

## Requisito
`keyless-deck` + `keyless-image` instalados.
